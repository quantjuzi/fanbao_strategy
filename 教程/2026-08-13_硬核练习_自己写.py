# -*- coding: utf-8 -*-
# 2026-08-13 硬核练习：自己写三个函数
#
# 规则：
# 1. 不翻答案文件，不复制粘贴，全部手打
# 2. 一次只做一题，做完先自己运行看结果
# 3. 卡住超过10分钟，把卡住的那一行发我，我给你提示
#    不要整篇发给我让我帮你写


# ============================================================
# 练习1：手动算 N 日均线（不用 pandas，用循环）
# ============================================================
def manual_ma(prices: list, window: int) -> list:
    """
    输入：收盘价列表，例如 [1, 2, 3, 4, 5]；窗口，例如 3
    输出：和 prices 一样长的列表
    - 前 window-1 个位置数据不够，填 None
    - 从第 window-1 个位置开始，每个值是最近 window 天的平均价

    期望：
    manual_ma([1, 2, 3, 4, 5], 3)
    -> [None, None, 2.0, 3.0, 4.0]
    """
    result = []
    # TODO: 在这里写你的代码
    # 提示：for i in range(len(prices))，用切片 prices[i - window + 1 : i + 1]
    return result


# ============================================================
# 练习2：算一笔交易的真实盈亏（费用规则你已经学过了）
# ============================================================
def calc_trade_pnl(
    buy_price: float,
    sell_price: float,
    shares: int,
    commission_rate: float = 0.00025,
    stamp_rate: float = 0.0005,
) -> dict:
    """
    输入：买入价、卖出价、股数、佣金费率、印花税率
    输出：字典，包含三项：
      "总费用": 买入佣金 + 卖出佣金 + 印花税
      "净盈亏": 卖出到手钱 - 买入总成本
      "收益率%": 净盈亏 / 买入总成本 * 100

    费用规则：
    - 佣金 = 成交额 x 费率，但最低 5 元，买卖各收一次
    - 印花税 = 卖出成交额 x 费率，只在卖出收
    - 过户费先不算，别把自己绕晕

    验证用例（手算一遍再跑代码）：
    buy_price=10, sell_price=11, shares=1000
    -> 买入成本 10005，卖出到手 10989.5，净盈亏 984.5，收益率约 9.84
    """
    # TODO: 在这里写你的代码
    # 提示：先算买入成交额、卖出成交额，再套费用规则
    return {}


# ============================================================
# 练习3：统计"连续上涨"的最长天数（面试常考，也是盘感代码化）
# ============================================================
def max_consecutive_up(daily_returns: list) -> int:
    """
    输入：每日收益率列表，例如 [0.02, -0.01, 0.01, 0.01, 0.03, -0.02]
    输出：连续为正的最长天数

    期望：
    max_consecutive_up([0.02, -0.01, 0.01, 0.01, 0.03, -0.02]) -> 3
    max_consecutive_up([-0.01, -0.02, 0.0]) -> 0
    """
    # TODO: 在这里写你的代码
    # 提示：用一个"当前连续天数"和一个"历史最长天数"
    return 0


# ============================================================
# 运行区：写完后运行，对照期望值检查
# ============================================================
if __name__ == "__main__":
    print("练习1:", manual_ma([1, 2, 3, 4, 5], 3))
    print("练习2:", calc_trade_pnl(10.0, 11.0, 1000))
    print("练习3:", max_consecutive_up([0.02, -0.01, 0.01, 0.01, 0.03, -0.02]))
